/*Program to print the Binary equivalent of a no. using Bitwise shift and logical AND operator*/
#include<stdio.h>
#include<conio.h>
void main()
     {
     unsigned int x,y=1,i;
     clrscr();
     printf("Enter a no:");
     scanf("%d",&x);
     y=y<<8*sizeof(x);i++);
     if(x&y)
       printf("1");
     else
	printf("0");
     getch();
     }