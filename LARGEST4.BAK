/*Program to find the largest no. among four digit no. using nested if...else logical statement and conditional operator*/
#include<stdio.h>
#include<conio.h>
void main()
     {
     int a,b,c,d;
     clrscr();
     printf("Enter 4 digit no.:");
     scanf("%d%d%d%d",&a,&b,&c,&d);
     if(a>b)
       if(a>c)
	 if(a>d)
	   printf("%d is largest",a);
	 else
	     printf("%d is largest",d);
       else
	   if(c>d)
	      printf("%d is largest",c);
	   else
	       printf("%d is largest",d);
     else
	 if(b>c)
	    if(b>d)
	       printf("%d is largest",b);
	    else
		printf("%d is largest",d);
	 else if(c>d)
		printf("%d is largest",c);
	      else
		  printf("%d is largest",d);
     getch();
     }

