/*Program to check that a given year is a leap year or not using Nested if...else operator*/

#include<stdio.h>
#include<conio.h>
void main()
     {
     int year;
     clrscr();
     printf("Enter year:");
     scanf("%d",&year);
     if(year%100==0)
       if(year%400==0)
	  printf("Leap year");
       else
	   printf("Not a leap year");
     else
	 if(year%4==0)
	    printf("Leap year");
	 else
	     printf("Not a leap year");
     getch();
     }